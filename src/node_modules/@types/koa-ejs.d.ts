import * as Koa from 'koa'

declare function render(app: Koa, options?: {
  root?: string
  layout?: string
  viewExt?: string
  cache?: boolean
  debug?: boolean
}): void

declare namespace render {}

export = render

declare module 'koa' {
  interface Context {
    render(viewPath: string): Promise<void>
  }
}
